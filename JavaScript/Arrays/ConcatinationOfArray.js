let arr = [1,1,2,3,"Anand",false];
let brr = [32,53,"Kumar",true];

// After concat we have to store concatinated array to array to get desired result
let crr = arr.concat(brr);
console.log(arr,brr,crr);   // Printing 3 array simultaneously
